type Query {
  "List of songs"
  songs: [Song]!
  song(id: ID!): Song
  currentSlide: Slide

  themes: [Theme]!

  "Can be 'desktop' or 'cloud'"
  applicationMode: String
  displays: [Display]
}

type Subscription {
  currentVerseChanged: String
  hello: String
}
